#include <iostream>
#include <vector>
using namespace std;


int main()
{
    int n;
    cin >> n;
    vector<int> s(n);
    vector<int> b(n);
    for(int i = 0 ; i < n ; i++)
    {
        cin >> s[i] >> b[i];
    }


}